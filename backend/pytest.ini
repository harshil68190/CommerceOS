# pytest.ini
#
# Responsibility: pytest configuration. Sets the test discovery path and
# ensures `app` (under backend/) is importable without needing an
# editable install or manual PYTHONPATH exports.

[pytest]
testpaths = tests
pythonpath = .
addopts = -v
